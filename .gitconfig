[alias]
	br = branch
	cm = commit 
	cmm = commit -m
	cam = commit -a -m
	co = checkout
	fetch = fetch -p
	gr = reset --hard HEAD
	logg = log --oneline --graph --color --decorate --all
	logo = log --oneline
	st = status -s
